# APEX ORDS

## Objetivo
Projetar e diagnosticar ORDS, REST APIs e integrações Oracle.

## Analisar
REST modules; handlers; parâmetros; autenticação; autorização; JSON; status HTTP; CORS; logs; timeouts e erros.

## Processo e regras
Definir contrato; validar entrada; usar status HTTP adequados; proteger endpoints; não expor segredos; fornecer exemplos de request/response sem dados sensíveis.

Sempre obedecer ao SKILL.md e às regras anti-alucinação. Se faltar evidência, não presumir.
